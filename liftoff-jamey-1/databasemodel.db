/*"PRAGMA foreign_keys = 0;

CREATE TABLE sqlitestudio_temp_table AS SELECT *
                                          FROM bookreaders;

DROP TABLE bookreaders;

CREATE TABLE bookreaders (
    id                 INTEGER PRIMARY KEY,
    first_name         TEXT,
    last_name          TEXT,
    administrator      INTEGER,
    administrator_code INTEGER
);

INSERT INTO bookreaders (
                            id,
                            first_name,
                            last_name,
                            administrator,
                            administrator_code
                        )
                        SELECT id,
                               first_name,
                               last_name,
                               administrator,
                               administrator_code
                          FROM sqlitestudio_temp_table;

DROP TABLE sqlitestudio_temp_table;

PRAGMA foreign_keys = 1;
*/

